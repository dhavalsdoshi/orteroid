<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent">

    <Button
            android:id="@+id/row_text"
            android:layout_width="500dip"
            android:layout_height="wrap_content"
            android:padding="4dip"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="5dp"
            android:layout_marginRight="10dip"
            android:layout_marginLeft="10dip"
            android:textSize="20dip"
            android:background="@drawable/sticky"
            style="?android:attr/borderlessButtonStyle"
            android:onClick="editIdea" android:longClickable="true"/>
    <ImageButton
            android:layout_width="30dp"
            android:layout_height="25dp"
            android:id="@+id/deleteButton"
            android:clickable="true" android:onClick="deleteIdea"
            android:layout_alignRight="@+id/row_text" android:layout_alignTop="@+id/row_text"
            android:src="@android:drawable/ic_menu_close_clear_cancel" android:visibility="invisible"
            android:contentDescription="@string/delete"/>

</RelativeLayout>